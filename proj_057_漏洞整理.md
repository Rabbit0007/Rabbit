# proj_057 / 后勤1 漏洞整理

生成时间：2026-08-08 CST

## 项目概况

- 项目名称：后勤1
- 项目 ID：proj_057
- 当前状态：completed
- 测试入口：`http://202.194.7.80:8060/sddx-admin/#/login`

## 总体结论

本次测试已确认 4 个漏洞，包含 2 个高危、1 个中危、1 个低危问题。当前已确认问题主要集中在跨域读取、管理员敏感数据暴露、后台写接口校验不足以及默认组件暴露。

截至当前测试收敛结果，暂未复现以下高影响链路：

- 无需管理员交互的现有会话自动接管
- JWT 校验绕过
- 对象级越权
- SSRF
- 文件上传至服务器可利用链
- 命令执行
- 主机权限获取

在缺少服务器侧部署配置、WAR/WEB-INF 内容、依赖清单、运行日志或主机访问证据的前提下，当前没有足够证据证明存在从应用层继续扩大到服务器文件、进程、命令执行或主机权限的可复现路径。

## 漏洞清单

### 1. 高危：CORS 任意 Origin 回显并允许凭据，导致管理员认证响应及受保护数据可被跨域读取

#### 问题概述

后端对攻击者指定的 `Origin` 原样回显，并返回允许凭据的跨域响应。经验证，在诱导管理员重新提交凭据的场景下，攻击者页面可以跨域读取登录响应以及后续权限数据。

#### 已确认结果

- 可跨域读取新签发的管理员 JWT
- 可跨域读取管理员权限相关受保护数据
- 可通过恶意页面配合管理员交互完成数据读取

#### 影响

攻击者可通过构造恶意站点，在管理员被诱导重新登录或主动提交令牌的情况下，读取管理员认证响应及其权限范围内的受保护数据，形成高风险的管理数据泄露场景。

#### 当前边界

当前尚未证明攻击者可以在无需管理员交互的情况下直接窃取现有管理员会话，因此该问题已确认高危，但尚未升级为稳定会话接管。

### 2. 高危：管理员接口过度暴露用户及业务敏感信息

#### 问题概述

多个管理员接口返回超出正常业务展示需求的敏感字段，包括账号相关敏感信息和业务运营数据。

#### 已确认结果

已确认可读取的信息包括但不限于：

- 用户 `password hash`
- `salt`
- 手机号
- 邮箱
- 设备相关标识和业务数据
- 部分订单及管理侧业务数据

#### 影响

一旦攻击者获得管理员访问能力，可批量读取高价值敏感信息，扩大用户隐私泄露、业务情报泄露和后续账号攻击风险。

#### 当前边界

当前已确认的问题是高价值敏感数据暴露，但暂未基于这些数据直接打通服务器权限获取链路。

### 3. 中危：多个管理写接口缺少字段完整性和对象存在性校验

#### 问题概述

部分后台 `create` 和 `update` 接口对空载荷、近空载荷以及对象存在性检查不足，存在服务端校验偏弱的问题。

#### 已确认结果

- 空 JSON 或近空 JSON 在部分接口可被接受
- 接口可产生可回滚的持久化副作用
- 部分对象存在性校验不足

#### 影响

攻击者在具备相应后台访问能力后，可制造异常配置、无效记录或业务污染，增加后台误操作及配置异常风险。

#### 当前边界

目前未复现通过这些弱校验接口进一步实现文件写入、远程资源引用、命令执行或服务器侧影响。

### 4. 低危：公开暴露 Tomcat 默认文档、示例应用及精确组件版本

#### 问题概述

目标公开暴露了 Tomcat 默认文档、示例应用以及精确版本信息，同时可识别部分默认组件暴露面。

#### 已确认结果

- 可访问 Tomcat 默认文档和示例资源
- 可识别精确组件版本
- 可确认默认示例应用和文档对外暴露

#### 影响

该问题会增加外部攻击者对目标技术栈、部署信息和后续攻击面的掌握程度，为进一步漏洞挖掘提供辅助信息。

#### 当前边界

当前未复现由该问题直接导致的未授权管理访问、文件写入、命令执行或主机权限获取路径。

## 最终结论

本次测试已完成当前证据条件下的漏洞收敛，确认目标主要存在以下四类问题：

1. CORS 跨域读取高危问题
2. 管理员敏感数据高危暴露问题
3. 管理写接口弱校验问题
4. 默认组件与版本信息暴露问题

当前测试未证明存在可稳定复现的服务器权限获取路径。若需继续向服务器侧高影响链路推进，需补充以下任一类证据后再继续验证：

- Tomcat 或应用部署配置
- WAR/WEB-INF 内容
- 依赖清单
- 运行日志
- 主机或服务器侧授权访问证据

