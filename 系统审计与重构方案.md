# Cairn_Y AI 自动化渗透系统 - 审计与重构方案

> 审计日期：2026-09-02  
> 审计范围：基于 Cairn 架构改进的 AI 自动化渗透引擎  
> 参考文档：《AI 自动化渗透架构 Cairn 满分登顶 TsecBench Cybench —— 众人向左，我偏向右.md》

---

## 一、系统架构现状分析

### 1.1 核心架构组件

当前系统实现了以下核心组件：

#### ✅ 已实现的核心概念

1. **FGS 图 (Fact-Goal-Step Graph)**
   - ✅ **Fact**：已实现，存储在 `facts` 表，描述已确认的事实和世界状态
   - ✅ **Goal**：已实现，存储在 `goals` 表，描述完成条件和子目标
   - ✅ **Step**：已实现，存储在 `steps` 表，描述探索动作和因果过程
   - ✅ **Finding**：已实现，存储在 `findings` 表，记录搜索过程中发现的漏洞

2. **Decide & Execute 双活动模型**
   - ✅ **Decide**：已实现 (`tasks/decide.py`)，负责分析 FGS 图并决策下一步
   - ✅ **Execute**：已实现 (`tasks/execute.py`)，负责执行具体步骤并产生新事实

3. **工程化组件**
   - ✅ Worker 管理：支持多种 worker 类型
   - ✅ 容器隔离：Docker 容器化执行环境
   - ✅ 心跳机制：任务健康检查和超时处理
   - ✅ 调度循环：自动调度 Decide 和 Execute 任务
   - ✅ Web 界面：基于 React 的前端界面

### 1.2 架构过渡状态识别

⚠️ **发现的架构不一致问题**

系统目前处于从 **Cairn（旧架构）** 向 **Cairn_Y（新架构）** 的过渡状态：

1. **命名混淆**：
   - 旧架构概念 `Intent` 与新架构概念 `Step` 并存
   - 数据库中 `intents` 表已迁移到 `steps` 表
   - 代码中大量存在 `Intent` 别名和兼容性代码
   - `reason` 活动已重命名为 `decide`，但向后兼容代码依然存在

2. **遗留代码**：
   ```python
   # models.py 中的混淆
   class Intent(BaseModel):  # 旧架构
       intent_id: str | None = None  # backward compat
       
   class Step(BaseModel):   # 新架构
       ...
       
   CreateIntentRequest = CreateStepRequest  # 别名兼容
   ReasonCheckpoint = DecideCheckpoint      # 别名兼容
   ```

3. **提示词版本混杂**：
   - `prompts/default/` 包含新架构提示词 (decide.md, execute.md)
   - `prompts/mock/` 包含旧架构提示词 (reason.md, explore.md)
   - Bootstrap 任务仍在使用混合的术语

---

## 二、核心问题清单

### 2.1 架构一致性问题 🔴 高优先级

| 问题ID | 问题描述 | 影响范围 | 严重程度 |
|--------|---------|---------|---------|
| A-001 | Intent/Step 概念混用导致理解困难 | 全局 | 高 |
| A-002 | Reason/Decide 命名不统一 | API、数据库、代码 | 高 |
| A-003 | 旧架构 prompts (reason, explore) 仍存在 | 提示词系统 | 中 |
| A-004 | Bootstrap 任务逻辑不符合 FGS 图理念 | 初始化流程 | 中 |
| A-005 | 数据库迁移代码应该清理 | db.py | 低 |

### 2.2 架构设计问题 🟡 中优先级

| 问题ID | 问题描述 | 文档理念对比 | 建议 |
|--------|---------|------------|-----|
| D-001 | Decide 和 Execute 被实现为独立函数而非统一的 Agent Loop | 文档：注入不同 Tool 的同一运行器 | 需要抽象统一 |
| D-002 | 缺少明确的 Tool 注入机制 | 文档：Decide 只能操作图，Execute 只能操作世界 | 需要权限控制 |
| D-003 | Worker 驱动层过于依赖外部 SDK | 文档：倾向自写轻量 Agent Loop | 可选重构 |
| D-004 | 缺少 Finding 定义机制 | 文档：Finding 可以动态定义 | 扩展性不足 |
| D-005 | 子目标 (Sub Goal) 的优先级调度不够智能 | 文档：阶段性平稳推进 | 需要优化 |

### 2.3 工程实现问题 🟢 低优先级

| 问题ID | 问题描述 | 位置 | 影响 |
|--------|---------|-----|-----|
| E-001 | 日志中文硬编码，国际化支持缺失 | decide.py, execute.py | 可维护性 |
| E-002 | 错误处理过于简化，缺少详细错误分类 | tasks/*.py | 调试困难 |
| E-003 | 配置文件缺少验证和文档 | config.py | 使用门槛高 |
| E-004 | 前端与后端耦合度高 | frontend/src/ | 难以独立开发 |
| E-005 | 缺少完整的 API 文档 | routers/*.py | 集成困难 |

### 2.4 安全与合规问题 🔴 高优先级

| 问题ID | 问题描述 | 位置 | 风险等级 |
|--------|---------|-----|---------|
| S-001 | 授权边界检查不够完善 | project_scope.py | 高 |
| S-002 | 容器逃逸风险未充分防护 | runtime/containers.py | 高 |
| S-003 | 密钥管理机制不够安全 | dispatch.yaml 占位符展开 | 中 |
| S-004 | 缺少审计日志完整性保护 | 全局 | 中 |
| S-005 | Worker 输出未进行安全过滤 | output_parser.py | 中 |

---

## 三、重构方案

### 3.1 第一阶段：架构统一与概念清理 (1-2周)

#### 3.1.1 清理遗留概念

**目标**：完全移除 Intent/Reason 等旧架构概念，统一为 FGS 图术语

**具体任务**：

1. **代码清理**
   ```python
   # 删除所有 Intent 相关别名
   # 删除 CreateIntentRequest = CreateStepRequest
   # 删除 ReasonCheckpoint = DecideCheckpoint
   # 删除 ProjectReason 类型
   ```

2. **数据库清理**
   ```python
   # 移除 _migrate_intents_to_steps 函数（假设所有实例已迁移）
   # 移除 projects 表的 reason_* 列（保留 decide_* 列）
   # 更新所有 SQL 注释，使用 FGS 术语
   ```

3. **API 统一**
   ```python
   # 移除 /api/projects/{id}/intents 端点（如果存在）
   # 确保所有端点使用 /steps、/goals、/facts、/findings
   # 统一错误消息和响应模型
   ```

4. **提示词清理**
   ```bash
   # 删除 prompts/mock/ 目录（如果不再使用）
   # 或者重命名为 prompts/legacy/ 并标记为已废弃
   ```

**验收标准**：
- [ ] 代码中不再出现 Intent/Reason 相关命名（除注释说明历史）
- [ ] 所有 API 端点使用 FGS 术语
- [ ] 数据库 schema 清晰反映 FGS 图结构

#### 3.1.2 统一 Agent Loop 抽象

**目标**：实现文档描述的"注入不同 Tool 和 Prompt 的统一运行器"

**设计方案**：

```python
# cairn/src/cairn/dispatcher/agent_loop.py

from dataclasses import dataclass
from typing import Protocol, Any

class Tool(Protocol):
    """Tool 协议定义"""
    def name(self) -> str: ...
    def description(self) -> str: ...
    def execute(self, **kwargs) -> Any: ...

@dataclass
class AgentContext:
    """Agent 运行上下文"""
    project_id: str
    graph_snapshot: str  # FGS 图的 YAML 表示
    available_tools: list[Tool]
    prompt_template: str
    session: str | None = None

class AgentLoop:
    """统一的 Agent Loop 实现"""
    
    def __init__(self, worker_driver, timeout: int):
        self.driver = worker_driver
        self.timeout = timeout
    
    def run(self, context: AgentContext) -> AgentResult:
        """
        运行 Agent Loop：
        1. 渲染提示词（注入 graph、tools）
        2. 调用 worker 执行
        3. 解析结果
        4. 验证输出（只能使用被注入的 tools）
        """
        pass

# 使用示例
def run_decide_activity(project_id: str, ...):
    tools = [
        ReadGraphTool(),
        CreateStepTool(),
        UpdateStepTool(),
        CreateGoalTool(),
        CompleteGoalTool(),
    ]
    context = AgentContext(
        project_id=project_id,
        graph_snapshot=export_yaml,
        available_tools=tools,
        prompt_template="decide.md",
    )
    loop = AgentLoop(worker_driver, timeout=300)
    return loop.run(context)

def run_execute_activity(project_id: str, step_id: str, ...):
    tools = [
        ReadGraphTool(),
        BashTool(),
        ReadFileTool(),
        WriteFileTool(),
        SubmitFactTool(),
        SubmitFindingTool(),
    ]
    context = AgentContext(
        project_id=project_id,
        graph_snapshot=export_yaml,
        available_tools=tools,
        prompt_template="execute.md",
    )
    loop = AgentLoop(worker_driver, timeout=600)
    return loop.run(context)
```

**优势**：
- ✅ 符合文档描述的架构理念
- ✅ Decide 和 Execute 共享同一运行机制
- ✅ Tool 权限清晰分离（Decide 不能操作文件系统，Execute 不能修改图结构）
- ✅ 易于添加新的活动类型（如 Analyze、Verify）

**实施步骤**：
1. 创建 `agent_loop.py` 和 `tools.py`
2. 重构 `decide.py` 使用新抽象
3. 重构 `execute.py` 使用新抽象
4. 保留 bootstrap 任务（待后续重构）
5. 全面测试

### 3.2 第二阶段：架构增强 (2-3周)

#### 3.2.1 增强 Finding 机制

**问题**：当前 Finding 结构固定为漏洞挖掘场景

**方案**：支持动态 Finding 定义

```python
# cairn/src/cairn/server/models.py

class FindingSchema(BaseModel):
    """Finding 的动态模式定义"""
    type_name: str  # 例如 "vulnerability", "flag", "credential"
    required_fields: list[str]
    optional_fields: list[str]
    severity_levels: list[str] | None = None

class ProjectMeta(BaseModel):
    id: str
    title: str
    status: str
    finding_schemas: list[FindingSchema] = []  # 新增

# 项目创建时可以指定 Finding 类型
class CreateProjectRequest(BaseModel):
    title: str
    origin: str
    goal: str
    finding_type: Literal["vulnerability", "ctf_flag", "custom"] = "vulnerability"
    custom_finding_schema: FindingSchema | None = None
```

#### 3.2.2 优化子目标调度

**方案**：实现智能的阶段性目标推进

```python
# cairn/src/cairn/dispatcher/scheduler/goal_scheduler.py

class GoalScheduler:
    """目标调度器：确保任务阶段性平稳推进"""
    
    def should_trigger_decide(self, project: ProjectDetail) -> bool:
        """
        判断是否应该触发 Decide：
        1. 有新的 Fact 产生
        2. 当前阶段的 Sub Goal 已完成
        3. 所有 Open Steps 都已完成
        4. 长时间无进展（死锁检测）
        """
        pass
    
    def select_next_step(self, open_steps: list[Step]) -> Step | None:
        """
        选择下一个要执行的 Step：
        1. 按 priority 排序
        2. 考虑 goal_id 关联（优先完成同一子目标）
        3. 负载均衡（避免同一 worker 过载）
        """
        pass
```

#### 3.2.3 Bootstrap 重构

**问题**：Bootstrap 不符合 FGS 图纯粹性

**方案**：将 Bootstrap 改为特殊的 Decide 触发

```python
# 旧方式：Bootstrap 是独立任务类型，直接生成 Fact
# 新方式：Bootstrap 触发一次特殊的 Decide，Decide 生成初始 Steps

def initialize_project(project_id: str, origin: str, goal: str):
    """
    项目初始化：
    1. 创建 origin 和 goal 两个初始 Fact
    2. 创建主 Goal
    3. 触发第一次 Decide（空白状态）
    4. Decide 分析 origin 和 goal，生成初始探索 Steps
    """
    client.create_fact(project_id, "origin", origin)
    client.create_fact(project_id, "goal", goal)
    client.create_goal(project_id, goal_description=goal)
    trigger_decide(project_id, trigger="bootstrap")
```

### 3.3 第三阶段：安全加固 (1-2周)

#### 3.3.1 容器安全增强

```python
# cairn/src/cairn/dispatcher/runtime/containers.py

def create_sandbox_container(project_id: str) -> str:
    """
    创建安全沙箱容器：
    1. 使用 gVisor/Kata 等安全容器运行时
    2. 限制网络访问（仅允许白名单目标）
    3. 限制资源（CPU、内存、磁盘）
    4. 禁用特权操作
    5. 只读文件系统 + tmpfs 临时目录
    """
    security_opts = [
        "no-new-privileges:true",
        "seccomp=default",
        "apparmor=docker-default",
    ]
    # 使用 runtime=runsc (gVisor) 或 runtime=kata-runtime
    pass
```

#### 3.3.2 授权边界完善

```python
# cairn/src/cairn/project_scope.py

class ScopePolicy:
    """增强的范围策略"""
    
    def __init__(self, origin: str, goal: str, hints: list[dict]):
        self.allowed_hosts = self._extract_hosts(origin)
        self.allowed_actions = self._infer_allowed_actions(goal, hints)
        self.forbidden_actions = ["rm -rf", "dd if=", "fork bomb", ...]
    
    def validate_command(self, command: str) -> tuple[bool, str | None]:
        """验证命令是否在授权范围内"""
        # 检查目标主机
        # 检查危险命令
        # 检查文件路径
        pass
    
    def validate_network_request(self, url: str) -> bool:
        """验证网络请求是否在授权范围内"""
        pass
```

#### 3.3.3 输出安全过滤

```python
# cairn/src/cairn/dispatcher/output_parser.py

class SecureOutputParser:
    """安全的输出解析器"""
    
    SENSITIVE_PATTERNS = [
        r'-----BEGIN .*PRIVATE KEY-----',
        r'password\s*=\s*["\'].*["\']',
        r'token\s*=\s*["\'].*["\']',
        # ... 更多敏感信息模式
    ]
    
    def parse_and_sanitize(self, output: str) -> dict:
        """
        解析输出并脱敏：
        1. 移除敏感信息
        2. 限制输出长度
        3. 验证 JSON 结构
        """
        sanitized = self._redact_sensitive_info(output)
        parsed = extract_json_object(sanitized)
        return parsed
```

### 3.4 第四阶段：文档与测试 (1周)

#### 3.4.1 完善文档

1. **架构文档**：`docs/architecture.md`
   - FGS 图详细说明
   - Decide & Execute 流程图
   - Tool 权限矩阵
   - Finding 类型定义

2. **API 文档**：`docs/api.md`
   - OpenAPI 规范
   - 示例请求/响应
   - 错误码说明

3. **部署文档**：`docs/deployment.md`
   - Docker Compose 配置
   - 环境变量说明
   - 安全配置建议

#### 3.4.2 测试覆盖

```python
# 单元测试
tests/
  ├── test_fgs_graph.py          # FGS 图逻辑
  ├── test_agent_loop.py         # Agent Loop 抽象
  ├── test_tools.py              # Tool 权限验证
  ├── test_decide.py             # Decide 决策逻辑
  ├── test_execute.py            # Execute 执行逻辑
  ├── test_finding.py            # Finding 机制
  └── test_security.py           # 安全边界检查

# 集成测试
integration_tests/
  ├── test_end_to_end.py         # 完整流程测试
  ├── test_ctf_scenario.py       # CTF 场景
  ├── test_pentest_scenario.py   # 渗透测试场景
  └── test_container_isolation.py # 容器隔离测试
```

---

## 四、性能与成本优化建议

### 4.1 提示词优化

根据文档提到的"从 7000 元降到 50 元以下"，重点优化：

1. **减少冗余上下文**
   ```python
   # 当前：每次都传递完整 FGS 图
   # 优化：只传递相关子图
   def get_relevant_subgraph(project_id: str, step_id: str) -> str:
       """只返回与当前 Step 相关的 Facts 和 Goals"""
       pass
   ```

2. **增量更新而非全量重算**
   ```python
   # Decide 不需要每次都从头分析
   # 只需要分析自上次 Decide 以来的变化
   class DecideCheckpoint:
       fact_count: int
       hint_count: int
       goal_count: int
       open_step_count: int
       last_decide_at: str  # 新增
   ```

3. **压缩表示**
   ```yaml
   # 使用更紧凑的 YAML 表示
   # 旧格式：
   facts:
     - id: f001
       description: "Very long description..."
   
   # 新格式：
   facts:
     f001: "Short key finding"  # 长描述存文件，只传引用
   ```

### 4.2 本地模型支持

文档提到"也在研究本地模型方案"，建议：

```python
# cairn/src/cairn/dispatcher/workers/local_model.py

class LocalModelDriver(WorkerDriver):
    """本地模型驱动（如 Llama、Qwen 等）"""
    
    def __init__(self, model_path: str, context_length: int = 8192):
        self.model = self._load_model(model_path)
        self.context_length = context_length
    
    def build_execute(self, worker, prompt, session):
        """使用本地推理而非 API 调用"""
        pass
```

优点：
- 成本降为零（除硬件成本）
- 无 API 限流
- 数据完全本地化

---

## 五、实施路线图

### 时间线总览（6-8周）

```
Week 1-2: 阶段一 - 架构统一与概念清理
  ├─ Week 1: 代码清理 + 数据库清理
  └─ Week 2: API 统一 + 提示词清理 + 验收测试

Week 3-5: 阶段二 - 架构增强
  ├─ Week 3: Agent Loop 抽象 + Tool 系统
  ├─ Week 4: Finding 机制 + 子目标调度
  └─ Week 5: Bootstrap 重构 + 集成测试

Week 6-7: 阶段三 - 安全加固
  ├─ Week 6: 容器安全 + 授权边界
  └─ Week 7: 输出过滤 + 安全测试

Week 8: 阶段四 - 文档与测试
  ├─ 完善文档
  ├─ 测试覆盖
  └─ 性能优化
```

### 优先级建议

**必须做（P0）**：
- A-001, A-002: 概念统一
- S-001, S-002: 安全加固
- D-001: Agent Loop 抽象

**应该做（P1）**：
- D-002: Tool 权限控制
- D-004: Finding 扩展
- 3.4: 文档完善

**可以做（P2）**：
- E-001: 国际化
- D-003: Worker 驱动重写
- 4.2: 本地模型支持

---

## 六、风险评估

| 风险项 | 风险等级 | 缓解措施 |
|--------|---------|---------|
| 重构过程中引入新 bug | 中 | 完善测试覆盖，小步迭代 |
| 与现有系统不兼容 | 低 | 保留兼容层，逐步废弃 |
| 性能下降 | 低 | 基准测试，性能监控 |
| 安全加固影响功能 | 中 | 可配置的安全策略 |
| 时间超期 | 中 | 分阶段交付，P0 优先 |

---

## 七、度量指标

### 7.1 代码质量指标

- **架构一致性**：旧概念引用数 = 0
- **测试覆盖率**：> 80%
- **代码重复度**：< 5%
- **文档完整度**：100%（所有公开 API 有文档）

### 7.2 性能指标

- **单次 Decide 成本**：< 0.5元（使用 Claude Sonnet）
- **单次 Execute 成本**：< 2元
- **完整项目成本**：< 50元（63题靶场）
- **平均响应时间**：Decide < 30s, Execute < 120s

### 7.3 安全指标

- **容器逃逸测试**：0 成功逃逸
- **授权边界测试**：100% 越权请求被阻止
- **敏感信息泄露**：0 次
- **审计日志完整性**：100%

---

## 八、总结

### 当前系统评价

**优点**：
- ✅ 核心 FGS 图架构已实现且稳定
- ✅ 工程化完善（容器、调度、心跳）
- ✅ 前后端分离，Web 界面友好
- ✅ 已有一定的安全考虑

**问题**：
- ⚠️ 架构过渡期，概念混淆
- ⚠️ 缺少统一的 Agent Loop 抽象
- ⚠️ 安全加固需要进一步完善
- ⚠️ 文档和测试覆盖不足

### 重构价值

1. **理念回归**：完全贯彻 Cairn_Y 的极简主义和 FGS 图理念
2. **易维护性**：清晰的架构，一致的概念
3. **可扩展性**：统一的 Agent Loop，灵活的 Tool 系统
4. **安全性**：多层防护，审计完整
5. **成本优化**：更低的推理成本，支持本地模型

### 关键建议

**给架构师的建议**：
- 坚持 "Less Is More"，抵制复杂化的诱惑
- FGS 图是唯一的记忆和状态，不要引入其他状态管理
- Tool 权限清晰分离，Decide 和 Execute 职责明确

**给开发者的建议**：
- 先完成 P0 任务（概念统一 + 安全加固）
- 小步迭代，每次重构都有测试保护
- 重构时保持系统可运行，不要大爆炸式重写

**给使用者的建议**：
- 重构期间可能有 API 变动，注意版本兼容
- 安全加固后某些操作可能需要更明确的授权
- 新的 Finding 机制更灵活，可以自定义

---

## 附录

### A. 参考资料

1. 《AI 自动化渗透架构 Cairn 满分登顶 TsecBench Cybench —— 众人向左，我偏向右.md》
2. 《AUTHORIZED_SECURITY_SCOPE.md》 - 授权边界说明
3. Cairn 原始论文和代码：https://github.com/oritera/Cairn
4. TsecBench 评测平台文档

### B. 术语表

| 术语 | 说明 | 旧术语 |
|-----|------|--------|
| FGS Graph | Fact-Goal-Step 图，外化的记忆与世界状态 | Fact-Intent DAG |
| Decide | 决策活动，分析图并规划下一步 | Reason |
| Execute | 执行活动，执行步骤并产生新事实 | Explore |
| Step | 探索步骤，描述如何从事实产生新事实 | Intent |
| Goal | 目标，描述完成条件 | - |
| Finding | 搜索过程的结构化产物（如漏洞） | - |

### C. 联系方式

如有问题，请联系：
- 架构审计：[当前审计人]
- 系统维护：[项目负责人]
- 安全咨询：[安全团队]

---

**审计完成时间**：2026-09-02  
**下次审计计划**：重构完成后（预计 2026-10-30）
